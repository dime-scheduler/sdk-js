{
  "name": "pingpong",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "stage": "bun run stage.ts",
    "play": "bun run play.ts",
    "build": "bun build stage.ts && bun build play.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dimescheduler": "^0.4.3"
  }
}